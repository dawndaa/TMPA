name: tps
channels:
  - defaults
dependencies:
- python>=3.8
- pip>=21
- scikit-image>=0.18.1
- scipy
- pip:
   - transformers
   - ftfy
   - regex
   - tqdm
   - requests
   - timm
   - numpy
   - pandas
   - torch
   - pillow
   - opencv-python-headless
prefix: ./miniconda3/envs/tps
