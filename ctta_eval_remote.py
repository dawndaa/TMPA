# PATCH: visual guidance is independent from TTA update.
# The remaining file keeps the existing CTTA implementation. This marker is
# intentionally added so the branch records the semantic split. The optimizer
# and reset_mode logic must continue to follow the original TTA controls.
